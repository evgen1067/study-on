{% extends 'base.html.twig' %}

{% block title %}{{ 'Редактирование / ' ~ lesson.name ~ ' / ' ~ lesson.course.name ~ ' / StudyOn' }}{% endblock %}

{% block body %}
    <div class="container">
        <h1>Редактирование урока</h1>

        {{ include('lesson/_form.html.twig', {'button_label': 'Сохранить'}) }}

        <a class="btn btn-primary w-100 mb-3" href="{{ path('app_course_show', { 'id' : lesson.course.id }) }}">Обратно к курсу</a>

        {{ include('lesson/_delete_form.html.twig') }}
    </div>
{% endblock %}
