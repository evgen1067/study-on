{% extends 'base.html.twig' %}

{% block title %}{{ 'Создание урока / ' ~ course.name ~ ' / StudyOn' }}{% endblock %}

{% block body %}
    <h1 class="text-center m-3">Создание урока</h1>

    {{ include('lesson/_form.html.twig') }}

    <a class="btn btn-secondary mb-3 w-100" href="{{ path('app_course_show', { 'id' : course.id }) }}">Обратно к курсу</a>
{% endblock %}