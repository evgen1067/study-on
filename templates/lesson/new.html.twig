{% extends 'base.html.twig' %}

{% block title %}{{ 'Новый урок / ' ~ course.name ~ '/ StudyOn' }}{% endblock %}

{% block body %}
    <h1>Создайте новый урок прямо сейчас!</h1>

    {{ include('lesson/_form.html.twig') }}

    <a class="btn btn-primary mb-3 w-100" href="{{ path('app_course_show', { 'id' : course.id }) }}">Обратно к курсу</a>
{% endblock %}
